# IUB Valentine Confession Website V2 - Implementation Plan

A modern, romantic Valentine's Day confession posting website with anonymous confessions, Supabase backend, rich text editing, and comprehensive admin controls.

---

## Supabase Setup Guide

### Step 1: Create Supabase Project
1. Go to [supabase.com](https://supabase.com) and sign up/login
2. Click **"New Project"**
3. Choose your organization (or create one)
4. Enter project details:
   - **Name**: `iub-confession`
   - **Database Password**: Generate a strong password (save it!)
   - **Region**: Choose closest to Bangladesh (Singapore recommended)
5. Click **"Create new project"** and wait ~2 minutes

### Step 2: Get API Keys
1. Go to **Settings** → **API** in your Supabase dashboard
2. Copy these values for your `.env.local`:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role** key → `SUPABASE_SERVICE_ROLE_KEY`

### Step 3: Create Storage Bucket
1. Go to **Storage** in sidebar
2. Click **"New bucket"**
3. Name: `confessions`
4. Toggle **"Public bucket"** ON
5. Click **"Create bucket"**

### Step 4: Run Database Schema
After I create the schema file, run it in **SQL Editor** in Supabase.

---

## Configuration

| Setting | Value |
|---------|-------|
| Domain Restriction | `@iub.edu.bd` only |
| Max Image Size | 5MB (free tier allows 50MB, but 5MB is optimal) |
| Admin Emails | *To be configured before going live* |

> [!CAUTION]
> **Before Going Live Reminder**
> Update admin emails in `src/lib/admin-config.ts` before deployment!

---

## Tech Stack

| Category | Technology |
|----------|------------|
| Framework | Next.js 14 (App Router) |
| Language | TypeScript |
| Database | Supabase (PostgreSQL) |
| Auth | Supabase Auth (Email OTP) |
| Storage | Supabase Storage |
| Styling | Tailwind CSS |
| Animations | Framer Motion |
| Rich Text | Tiptap Editor |
| Icons | Lucide React |

---

## Design System

### Color Palette - "Modern Romantic"

```css
/* Primary Colors */
--rose-primary: #E11D48      /* Rose 600 - Main accent */
--rose-light: #FDA4AF        /* Rose 300 - Soft highlights */
--rose-dark: #9F1239         /* Rose 800 - Deep accents */

/* Neutral Backdrop */
--cream: #FFF1F2             /* Rose 50 - Background */
--blush: #FFE4E6             /* Rose 100 - Card backgrounds */

/* Card Accent Colors (rotating) */
--card-rose: #FFF1F2
--card-lavender: #F5F3FF
--card-peach: #FFF7ED
--card-mint: #F0FDF4
--card-sky: #F0F9FF

/* Text */
--text-primary: #1F2937      /* Gray 800 */
--text-secondary: #6B7280    /* Gray 500 */
```

### Typography
- **Headings**: Playfair Display (serif, romantic feel)
- **Body**: Inter (clean, modern readability)

---

## Proposed Changes

### Phase 1: Project Foundation

#### [NEW] Project Initialization

```bash
npx -y create-next-app@latest ./ --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
```

#### [NEW] [package.json](file:///home/neko/Desktop/Confession - V2/package.json)

Dependencies to install:
```json
{
  "dependencies": {
    "@supabase/supabase-js": "^2.x",
    "@supabase/ssr": "^0.x",
    "@tiptap/react": "^2.x",
    "@tiptap/starter-kit": "^2.x",
    "@tiptap/extension-underline": "^2.x",
    "@tiptap/extension-text-align": "^2.x",
    "@tiptap/extension-image": "^2.x",
    "framer-motion": "^11.x",
    "lucide-react": "^0.x",
    "date-fns": "^3.x"
  }
}
```

---

### Phase 2: Supabase Database Schema

#### [NEW] [supabase/schema.sql](file:///home/neko/Desktop/Confession - V2/supabase/schema.sql)

```sql
-- Users table (extends Supabase auth.users)
CREATE TABLE public.profiles (
  id UUID REFERENCES auth.users(id) PRIMARY KEY,
  username TEXT UNIQUE NOT NULL,
  email TEXT NOT NULL,
  is_admin BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Confessions table
CREATE TABLE public.confessions (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES public.profiles(id),
  content JSONB NOT NULL,           -- Tiptap JSON content
  image_url TEXT,
  color_variant INTEGER DEFAULT 0,   -- 0-4 for card colors
  status TEXT DEFAULT 'pending',     -- pending, approved, hidden, removed
  upvote_count INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  approved_at TIMESTAMPTZ
);

-- Votes table
CREATE TABLE public.votes (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES public.profiles(id),
  confession_id UUID REFERENCES public.confessions(id),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, confession_id)
);

-- Reports table
CREATE TABLE public.reports (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES public.profiles(id),
  confession_id UUID REFERENCES public.confessions(id),
  reason TEXT NOT NULL,
  status TEXT DEFAULT 'pending',  -- pending, resolved, dismissed
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Indexes for performance
CREATE INDEX idx_confessions_status ON public.confessions(status);
CREATE INDEX idx_confessions_created ON public.confessions(created_at DESC);
CREATE INDEX idx_confessions_upvotes ON public.confessions(upvote_count DESC);
```

#### Row Level Security Policies

```sql
-- Confessions: Anyone can view approved, authenticated can create
ALTER TABLE public.confessions ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Approved confessions are viewable by everyone"
  ON public.confessions FOR SELECT
  USING (status = 'approved');

CREATE POLICY "Authenticated users can create confessions"
  ON public.confessions FOR INSERT
  WITH CHECK (auth.uid() = user_id);

-- Votes: Authenticated users only
ALTER TABLE public.votes ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can manage their own votes"
  ON public.votes FOR ALL
  USING (auth.uid() = user_id);

-- Admin policies
CREATE POLICY "Admins can do everything on confessions"
  ON public.confessions FOR ALL
  USING (
    EXISTS (
      SELECT 1 FROM public.profiles
      WHERE id = auth.uid() AND is_admin = true
    )
  );
```

---

### Phase 3: Application Structure

```
src/
├── app/
│   ├── layout.tsx              # Root layout with providers
│   ├── page.tsx                # Homepage with feed
│   ├── globals.css             # Tailwind + custom styles
│   ├── login/
│   │   └── page.tsx            # Login page
│   ├── admin/
│   │   ├── layout.tsx          # Admin layout with auth check
│   │   ├── page.tsx            # Dashboard overview
│   │   ├── moderation/
│   │   │   └── page.tsx        # Post approval/management
│   │   ├── reports/
│   │   │   └── page.tsx        # Report management
│   │   └── stats/
│   │       └── page.tsx        # User statistics
│   └── api/
│       └── admin/
│           └── [...slug]/route.ts  # Admin API routes
├── components/
│   ├── layout/
│   │   ├── Header.tsx          # Site header with logo + auth
│   │   ├── Footer.tsx          # Site footer
│   │   └── AnimatedContainer.tsx
│   ├── feed/
│   │   ├── ConfessionCard.tsx  # Individual confession display
│   │   ├── ConfessionFeed.tsx  # Feed with infinite scroll
│   │   ├── FeedToggle.tsx      # Featured/Recent toggle
│   │   └── UpvoteButton.tsx    # Animated upvote
│   ├── post/
│   │   ├── CreatePostModal.tsx # Modal with editor
│   │   ├── RichTextEditor.tsx  # Tiptap implementation
│   │   ├── ImageUpload.tsx     # Supabase storage upload
│   │   └── ReportModal.tsx     # Report submission
│   ├── auth/
│   │   ├── LoginForm.tsx       # Email OTP login
│   │   └── AuthProvider.tsx    # Auth context
│   ├── admin/
│   │   ├── ModerationTable.tsx # Post management table
│   │   ├── ReportsList.tsx     # Reports display
│   │   ├── StatsCards.tsx      # Statistics overview
│   │   └── AdminSidebar.tsx    # Admin navigation
│   └── ui/
│       ├── Button.tsx          # Animated button
│       ├── Modal.tsx           # Animated modal
│       ├── Toast.tsx           # Notification toasts
│       └── LoadingSpinner.tsx  # Loading states
├── lib/
│   ├── supabase/
│   │   ├── client.ts           # Browser client
│   │   ├── server.ts           # Server client
│   │   └── middleware.ts       # Auth middleware
│   ├── hooks/
│   │   ├── useConfessions.ts   # Confession CRUD
│   │   ├── useVotes.ts         # Vote management
│   │   ├── useAuth.ts          # Auth hook
│   │   └── useAdmin.ts         # Admin operations
│   └── utils/
│       ├── algorithms.ts       # Featured post algorithm
│       └── colors.ts           # Card color utilities
└── types/
    └── database.ts             # Supabase type definitions
```

---

### Phase 4: Core Components

#### [NEW] [src/components/feed/ConfessionCard.tsx](file:///home/neko/Desktop/Confession - V2/src/components/feed/ConfessionCard.tsx)

Key features:
- Animated card with Framer Motion (scale, opacity transitions)
- Color variants based on confession index
- Rich text content rendering
- Upvote button with animation
- Report button
- Time ago display

#### [NEW] [src/components/post/RichTextEditor.tsx](file:///home/neko/Desktop/Confession - V2/src/components/post/RichTextEditor.tsx)

Features:
- Tiptap editor with SSR-safe configuration (`immediatelyRender: false`)
- Bold, Italic, Underline formatting
- Text alignment (left, center, right, justify)
- Emoji picker integration
- Character limit display

#### [NEW] [src/components/post/ImageUpload.tsx](file:///home/neko/Desktop/Confession - V2/src/components/post/ImageUpload.tsx)

Features:
- Drag and drop support
- Image preview
- Supabase Storage integration
- File size/type validation
- Upload progress indicator

---

### Phase 5: Animations (Framer Motion)

All animations will follow these patterns:

```typescript
// Page transitions
const pageVariants = {
  initial: { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  exit: { opacity: 0, y: -20 }
};

// Card hover effects
const cardVariants = {
  initial: { scale: 1 },
  hover: { scale: 1.02, boxShadow: "0 10px 30px rgba(225, 29, 72, 0.15)" },
  tap: { scale: 0.98 }
};

// Upvote animation
const heartVariants = {
  initial: { scale: 1 },
  clicked: { 
    scale: [1, 1.3, 1],
    transition: { duration: 0.3 }
  }
};

// Modal animations
const modalVariants = {
  hidden: { opacity: 0, scale: 0.95 },
  visible: { opacity: 1, scale: 1 }
};

// Stagger children
const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: { staggerChildren: 0.1 }
  }
};
```

---

### Phase 6: Featured Post Algorithm

```typescript
// Featured score calculation
function calculateFeaturedScore(confession: Confession): number {
  const ageInHours = (Date.now() - new Date(confession.created_at).getTime()) / 3600000;
  const gravity = 1.8; // Decay factor
  
  // Wilson score for upvotes (handles low vote counts)
  const upvotes = confession.upvote_count;
  const n = upvotes + 1; // Avoid division by zero
  const z = 1.96; // 95% confidence
  const phat = upvotes / n;
  const wilsonScore = (phat + z*z/(2*n) - z * Math.sqrt((phat*(1-phat)+z*z/(4*n))/n)) / (1+z*z/n);
  
  // Combine recency with engagement
  return wilsonScore / Math.pow(ageInHours + 2, gravity);
}
```

---

### Phase 7: Admin Dashboard

#### [NEW] [src/app/admin/page.tsx](file:///home/neko/Desktop/Confession - V2/src/app/admin/page.tsx)

Dashboard showing:
- Total confessions (pending/approved/hidden/removed)
- Total users
- Reports requiring attention
- Recent activity feed

#### [NEW] [src/app/admin/moderation/page.tsx](file:///home/neko/Desktop/Confession - V2/src/app/admin/moderation/page.tsx)

Features:
- Filter by status (pending, approved, hidden, removed)
- Preview confession content
- Quick action buttons (approve, hide, remove)
- Bulk actions support

#### [NEW] [src/app/admin/reports/page.tsx](file:///home/neko/Desktop/Confession - V2/src/app/admin/reports/page.tsx)

Features:
- List of pending reports with confession preview
- Reason display
- Actions: resolve (take action), dismiss

---

## Verification Plan

### Automated Browser Tests

After implementation, I will use the browser tool to verify:

1. **Homepage Loading**
   - Navigate to `http://localhost:3000`
   - Verify Valentine theme colors and styling
   - Check for feed loading animation
   - Verify confessions display without login

2. **Authentication Flow**
   - Click login button
   - Verify email input restricts to @iub.edu.bd domain
   - Test OTP flow (mock mode for development)

3. **Create Confession Flow**
   - Login as test user
   - Open create confession modal
   - Verify rich text editor loads
   - Test formatting buttons (bold, italic, underline)
   - Test image upload
   - Submit confession and verify pending status

4. **Upvote Functionality**
   - Click upvote on a confession
   - Verify animation plays
   - Verify count updates
   - Test upvote toggle (un-upvote)

5. **Report Functionality**
   - Click report button
   - Verify modal opens
   - Submit report with reason

6. **Admin Dashboard**
   - Navigate to `/admin`
   - Verify admin-only access
   - Test moderation actions
   - Test report resolution

### Manual Verification (User)

> [!TIP]
> After I complete the implementation, please verify:

1. **Visual Design Review**
   - Does the Valentine theme look modern and romantic (not pixel art)?
   - Are the card colors visually pleasing?
   - Do animations feel smooth and not overwhelming?

2. **Supabase Integration**
   - Are confessions persisting correctly?
   - Are images uploading to Supabase Storage?
   - Is authentication working with your @iub.edu.bd email?

3. **Mobile Responsiveness**
   - Does the site work well on mobile devices?

---

## Implementation Order

```mermaid
graph TD
    A[Phase 1: Project Setup] --> B[Phase 2: Database Schema]
    B --> C[Phase 3: Auth & Supabase Client]
    C --> D[Phase 4: Core UI Components]
    D --> E[Phase 5: Feed & Confessions]
    E --> F[Phase 6: Create Post Modal]
    F --> G[Phase 7: Voting & Reporting]
    G --> H[Phase 8: Admin Dashboard]
    H --> I[Phase 9: Animations & Polish]
    I --> J[Phase 10: Testing & Verification]
```

---

## Ready to Implement

All questions have been answered. Ready to proceed with implementation once approved.
